[pytest]
testpaths = tests
python_files = test_*.py
addopts = --cov=src --cov=server --cov-report=term-missing --cov-report=html:coverage_html
markers =
    gpu: requires GPU and CUDA
    ai_models: requires downloaded AI model weights
    slow: tests that take > 30 seconds
