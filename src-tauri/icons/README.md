# Tauri Icons

The icons in this directory were generated during project initialization.
To update the icons, replace `icon.png` and run:

```bash
npm run tauri icon
```

The current icons are functional placeholders. A final high-resolution brand set should replace `icon.png` (min 512x512) before production distribution.
