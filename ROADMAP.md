# 🚀 Local Curator Prime - Future Update Plan (Roadmap)

Local Curator Primeをさらに洗練されたプロフェッショナルなツール・アプリケーションへと引き上げるための、今後のアップデートと改善計画をまとめました。
「エンジニア視点（アーキテクチャ・堅牢性）」と「デザイナー視点（UI/UX・体験）」の2つの軸で分類しています。

---

## 🛠 1. エンジニアリング & アーキテクチャ強化 (Engineering & Architecture)

ユーザーに意識させない裏側の堅牢性と、アプリケーションとしての完成度を高める計画です。

### [✅ 完了] 1.1 デプロイメントと起動プロセスの統合 (Desktop App Launcher)
*   **現状のステータス**: 
    *   ダブルクリックで全環境が立ち上がる `start.bat` (Windows) および `start.sh` (Linux/Mac) を実装済み。
*   **今後の計画**:
    *   **Tauri** または **Electron** を用いて、バックエンドとフロントエンドを内包した1つのデスクトップアプリケーション（`.exe` や `.AppImage`）としてパッケージ化。

### [🛠 Backend完了] 1.2 高度なモデル管理システム (Model Manager)
*   **現状のステータス**:
    *   FastAPI側のモデル管理クラス (`ModelManager`) およびダウンロードAPIを実装済み。
*   **今後の計画**:
    *   UI（設定画面や初回セットアップ画面）に**モデルマネージャー**を追加し、モデルのダウンロード進捗（プログレスバー、完了ETA）を視覚化。
    *   ユーザーがAIモデルの保存先ドライブ（Dドライブなど大容量領域）をUIから設定できるように改善（Backendにはフック実装済み）。

### [✅ 完了] 1.3 スキャン非同期ジョブキューとレジューム機能 (Async Job Queue & Resume)
*   **現状のステータス**:
    *   SQLiteベースのジョブ管理システム (`ScanJobManager`) を実装。
    *   アプリ再起動時の**レジューム（処理の再開）機能**を実装。
    *   エラーでスキップされたファイルのログ記録機能を実装。

### [🛠 Backend完了] 1.4 重複排除・類似画像検索 (Deduplication & Reverse Search)
*   **現状のステータス**:
    *   **Backend**: Perceptual HashおよびCLIPベクトルを用いた重複検出エンジンの実装済み。
    *   **Reverse Search**: 画像アップロードによるセマンティック検索APIを実装済み。
*   **今後の計画**:
    *   重複画像をまとめて削除・統合できる**クリーナーUI**の提供。
    *   検索バーへの画像ドラッグ＆ドロップによる検索フロントエンドの実装。

### [🛠 Backend完了] 1.5 メタデータ書き戻し機能 (EXIF/IPTC Write-back)
*   **現状のステータス**:
    *   AIが付与したタグやディスクリプションを **XMP (Sidecar) 形式** または **EXIF形式** で直接書き込む `MetadataExporter` を実装済み。
*   **今後の計画**:
    *   UIから特定ファイルまたはライブラリ全体のエクスポートを実行できるインターフェースの実装。

---

## 🧠 2. AIモデル・推論エンジンの進化 (AI Models & Inference)

完全ローカル・プライバシー保護の原則を維持したまま、認識精度と検索体験を劇的に引き上げるためのコアエンジン入れ替え計画です。ユーザーの要望に基づき、段階的なアップデートを予定しています。

### [✅ 完了] 2.1 高精度イラストタグ付けモデルへの移行 (Advanced Illustration Tagging)
*   **現状の課題**: 既存のタグ付けシステム（DeepDanbooruやWD14）は優秀ですが、最新の微細な画風や特定のキャラクター特徴の把握において限界があります。
*   **実装計画**:
    *   **JoyTag** (または同等の最新の小規模ViTベースモデル) の導入。
    *   Waifu Diffusion (WD14 Tagger) と比較して誤認識が少なく、ポーズ、服装の質感、背景要素まで非常に正確に抽出します。
    *   **利点**: ハードウェア要件を抑えつつ、イラスト検索の「タグの正確性」を格段に向上させます。

### [✅ 完了] 2.2 小規模VLMによるキャプション強化 (Enhanced Captioning)
*   **実装計画**:
    *   **Florence-2** のような非常に軽量（2億〜7億パラメータ）かつ強力なVision-Language Modelの導入。
    *   **利点**: イラストだけでなく、実写（風景、オブジェクト）のスキャン時にも、FAISSベクトル検索（文章でのセマンティック検索）の精度を高める詳細なキャプションを高速に生成します。

### [✅ 完了] 2.3 動画・実写における特定の人物（顔）認識と検索 (Face Retrieval Search)
*   **現状の課題**: 「特定の家族が映っている動画だけを探す」「同じ人が映っている異なるメディアをまとめる」という機能がありません。
*   **実装計画**:
    *   **InsightFace (RetinaFace + ArcFace)** による高度な顔認識（Face Recognition）機能の実装。現在オープンソースで最高の精度（99.8%超）を誇るモデルです。
    *   動画のキーフレームから顔領域を抽出し、その顔の「特徴ベクトル」をFaissに保存します。
    *   **利用フロー**: UI上で「この顔を[Person A]として登録」するだけで、過去・未来のスキャンファイルから同一人物が映っている写真・動画の該当シーンだけを正確に抽出（Face Retrieval）できるようになります。

---

## 🎨 3. UI/UX・デザイン体験の向上 (Design & User Experience)

プレミアムなプロダクト体験を提供し、利用者のワークフローを直感的にする計画です。

### 2.1 オンボーディングの洗練 (Premium Setup Experience)
*   **現状の課題**: 初回起動時に何をすればいいか、パスをどう入力すればいいか迷う可能性がある。
*   **実装計画**:
    *   **UIネイティブなフォルダピッカー**: File System Access APIやバックエンドAPIを利用した「フォルダを参照」ダイアログの実装。パスの手入力を不要に。
    *   **セットアップウィザード**: 初回起動時に美しいスライドショーで機能を案内し、テーマ（ライト/ダーク）や利用環境（GPU優先等のプロファイル設定）を選択させる体験の追加。

### 2.2 ハイブリッドクエリビルダー (Advanced Hybrid Search UI)
*   **現状の課題**: セマンティック（自然言語）検索のみで、メタデータ（厳密なタグ一致）との複合検索がUIで表現されていない。
*   **実装計画**:
    *   「自然言語ベースのFAISS検索」と「属性ベースのSQLite検索（タグ、拡張子など）」を掛け合わせる高度な検索バー。
    *   `[Tag: Hatsune Miku]`, `[Exclude: .mp4]` のようなチップUIを検索バー内で作成し、残りのテキストを「夕暮れのステージ」としてAI検索できるようにする。

### 2.3 スマートアルバム機能 (Smart Dynamic Albums)
*   **実装計画**:
    *   よく使う高度な検索条件（例：「2024年の動画」かつ「雨のシーン」を含む）を**「スマートアルバム」として保存**。
    *   以後新しくファイルが追加された際、条件に合致すれば自動的にそのアルバムに追加される仕組み。

### [✅ 完了] 3.4 没入感のあるギャラリーUX (Immersive Masonry Gallery)
*   **現状のステータス**:
    *   **Pinterest風（Masonry）レイアウト**の導入済み。
    *   **動画のホバー・プレビュー**: リスト上の動画サムネイルにマウスカーソルを乗せた際の自動再生機能を実装済み。
